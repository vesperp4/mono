# Base URL of the backend API (portal-api Container App). The signup form and the
# email confirmation page call `${NEXT_PUBLIC_API_URL}/api/v1/members*`.
# Local dev (matches the API's default bind on port 8080):
NEXT_PUBLIC_API_URL=http://localhost:8080
