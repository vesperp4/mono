# infra

Infrastructure, scripts, and IaC for this monorepo.

Planned contents:
- `docker/` — Dockerfiles per service
- `scripts/` — Dev and deployment scripts
- `terraform/` or `pulumi/` — Cloud infrastructure (when needed)
